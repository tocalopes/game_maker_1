 /// @description Insert description here
// You can write your code in this editor
with (other) { //other return the object that we collid with. 
	//if we pu the object, every single projet will be affected
	hp--;
	flash = 3;
	hit_from = other.direction;
}

instance_destroy();